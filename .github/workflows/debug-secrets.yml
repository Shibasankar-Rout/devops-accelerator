name: 🔐 Debug Secrets (Temporary)

on:
  workflow_dispatch:  # Allows you to run this manually from GitHub UI

jobs:
  reveal-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Show S3_BUCKET_NAME (⚠️ Only for Debugging)
        run: echo "S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}"

      - name: Show UPLOAD_BUCKET_NAME (⚠️ Only for Debugging)
        run: echo "UPLOAD_BUCKET_NAME: ${{ secrets.UPLOAD_BUCKET_NAME }}"

